<% provide(:title, 'Registered users') %>
<h1>Registered users <small>(<%= @users.count%>)</small></h1>

<%= will_paginate %>

<table class="table">
  <thead>
  <tr>
    <th></th>
    <th>User</th>
    <th>Tým</th>
    <th></th>
  </tr>
  <%= render @users %>
</table>

<%= will_paginate %>
