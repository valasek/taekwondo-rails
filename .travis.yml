language: ruby
sudo: false
rvm:
  - 2.3.1
branches:
    only:
        - master
before_script: "bundle exec rails db:drop db:create db:migrate"
script: "bundle exec rails test"

notifications:
  email: false